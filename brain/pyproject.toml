[project]
name = "scenic-brain"
version = "0.1.0"
requires-python = ">=3.10,<3.12"
dependencies = [
    "dashscope>=1.20.0",
    "pyyaml>=6.0",
    "pytest>=8.0",
]

[tool.setuptools.packages.find]
where = ["."]
